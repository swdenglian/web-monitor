{
  "name": "root",
  "private": true,
  "scripts": {
    "web-start": "cd packages/web;yarn start ;cd ..",
    "build": "lerna run --scope=monitor-collect build",
    "lerna": "lerna bootstrap"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1",
    "umi": "^3.4.2"
  }
}
